version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - cd netlify/functions/subscribe && npm install && cd ../../..
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - netlify/functions/subscribe/node_modules/**/*
